# Public-facing minimal Python dependencies for this repository.
# This file intentionally lists only top-level packages used by the codebase,
# not the full local Conda environment snapshot from AI_OS/requests.md.

numpy>=2.2
scipy>=1.15
pandas>=2.2
matplotlib>=3.10
torch>=2.0
smplx>=0.1.28